---
# tasks file for npm role

- name: "Detect Node.js package manager from project files"
  ansible.builtin.include_tasks: detect_package_manager.yml

- name: "Install packages using detected package manager"
  ansible.builtin.include_tasks: install_packages.yml
  when: npm_packages is defined
