---
# tasks file for installing Packer

- name: Block for Debian-based systems
  tags:
    - devops
    - packer
  block:
    - name: Install prerequisites for HashiCorp repository
      apt:
        name:
          - gpg
          - software-properties-common
        state: latest
        update_cache: yes

    - name: Add HashiCorp GPG key
      apt_key:
        url: https://apt.releases.hashicorp.com/gpg
        state: latest

    - name: Ensure HashiCorp repository is configured for Packer
      apt_repository:
        repo: "deb [arch=amd64] https://apt.releases.hashicorp.com {{ ansible_distribution_release }} main"
        state: latest

    - name: Install Packer on Debian via levonk.common.package
      levonk.common.package:
        name:
          - packer
        state: latest
  when: ansible_os_family == 'Debian'

- name: Block for macOS (Darwin)
  tags:
    - devops
    - packer
  block:
    - name: Ensure hashicorp/tap is tapped for Packer
      community.general.homebrew_tap:
        name: hashicorp/tap
        state: latest

    - name: Install Packer on macOS via levonk.common.package
      levonk.common.package:
        name:
          - packer
        state: latest
  when: ansible_os_family == 'Darwin'

- name: Install Packer on Windows
  tags:
    - devops
    - packer
  levonk.common.package:
    name:
      - packer
    state: latest
  win_chocolatey:
    name: packer
    state: latest
  when: ansible_os_family == 'Windows'
